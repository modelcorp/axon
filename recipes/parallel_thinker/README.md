# Parallel Thinker

Custom `BaseProgram` subclass: N parallel solvers → rewriter → selector. Read `program.py` end-to-end as the reference for writing your own non-linear program.

```bash
cd recipes/parallel_thinker/
./train_fsdp.sh
```
