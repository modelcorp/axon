# Conversion

HF → quantised checkpoint converters (INT4, MXFP8). Utility scripts, not training recipes.

```bash
python recipes/conversion/convert_hf_to_int4.py --model-dir <hf-id> --save-dir <dir>
python recipes/conversion/convert_hf_to_mxfp8.py --model-dir <hf-id> --save-dir <dir>
```
