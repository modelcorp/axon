# Core ML/AI Packages
torch==2.10.0
torchvision==0.25.0
torchaudio==2.10.0
transformers==5.5.3
accelerate>=1.13.0
sentence-transformers
peft
liger-kernel
huggingface_hub
# OpenAI client: imported at axon import time (axon.utils.llm_api / openai_client)
# and used for the OpenAI-compatible chat surface, so it is a core dependency
# rather than a transitive one pulled in via vLLM.
openai==2.41.1
# NOTE: flash-linear-attention and causal-conv1d require CUDA compilation
# and are installed in postinstall_script.sh (needed for Qwen3.5 GDN layers)
scipy==1.15.3
numpy

# Data Processing
datasets>=4.4.2,<5
polars
pandas
pyarrow>=19.0.0
dill
torchdata

# Ray & Distributed
ray[default]>=2.41.0
tensordict>=0.8.0,<=0.10.0,!=0.9.0

# Cloud & Infrastructure
google-cloud-aiplatform

# API Servers
uvicorn<0.41.0
# fastapi 0.137.0 introduced fastapi.routing._IncludedRouter, which lacks the
# `.path` attribute the vLLM OpenAI server expects when iterating routes — every
# rollout sampling call then 500s ("'_IncludedRouter' object has no attribute
# 'path'"). Cap below 0.137 (vLLM itself only needs >=0.115.0).
fastapi>=0.115.0,<0.137
fire

# Math & Science
latex2sympy2
latex2sympy2_extended
pylatexenc
mathruler
math_verify

# NOTE: Agent-specific packages (browsergym, swebench, firecrawl, etc.) are in
# requirements-agents.txt to avoid dependency conflicts with transformers>=5.3.0.

# Metrics
gradio
wandb
tensorboard

# Utilities
tabulate
pybind11
gymnasium
torch-memory-saver
codetiming
hydra-core==1.3.2
omegaconf==2.3.0
packaging>=20.0
qwen_vl_utils
psutil
click>=8.0
