# Agent-specific packages — install separately with: pip install -e .[agents]
# These are split from requirements-base.txt because browsergym's transitive
# dependency on text-generation pins huggingface-hub<1.0, which conflicts with
# transformers>=5.3.0 (requires huggingface-hub>=1.3.0).

# Code Evaluation
swebench
together
e2b_code_interpreter

# Web Agent
browsergym
firecrawl
